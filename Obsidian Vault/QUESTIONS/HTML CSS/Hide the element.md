Скрыть элемент, оставив занимаемое место на странице 

```css
div {
	opacity: 0; // прозрачность на ноль
}
```

ИЛИ:

```css
div {
	visibility: hidden;
}
```

А В СЛЕДУЮЩЕМ СЛУЧАЕ ОН НЕ БУДЕТ ЗАНИМАТЬ МЕСТО НА СТРАНИЦЕ:
```css
div {
	display: none;
}
```